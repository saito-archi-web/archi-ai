services:
  - type: web
    name: archi-ai
    runtime: node
    buildCommand: cd frontend && npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ANTHROPIC_API_KEY
        sync: false
